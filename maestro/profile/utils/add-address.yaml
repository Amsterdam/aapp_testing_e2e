appId: nl.amsterdam.app.dev
---
- tapOn:
    id: AddressAddButton
- waitForAnimationToEnd

- inputText: ${ADDRESS}

- extendedWaitUntil:
    visible:
      id: '^AddressSearchResult.*ButtonText$'

- tapOn:
    id: '^AddressSearchResult.*ButtonText$'
    text: ${ADDRESS}

- waitForAnimationToEnd
