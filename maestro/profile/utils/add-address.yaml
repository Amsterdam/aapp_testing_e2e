appId: nl.amsterdam.app.dev
---
- tapOn:
    id: AddressAddButton
- waitForAnimationToEnd

- inputText:
    text: ${ADDRESS}

- extendedWaitUntil:
    visible:
      id: '^AddressSearchResult.*ButtonText$'

- tapOn:
    id: '^AddressSearchResult.*ButtonText$'
    text: ${ADDRESS}

- waitForAnimationToEnd
